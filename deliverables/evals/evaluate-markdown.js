#!/usr/bin/env node
// evaluateMarkdown.js
// Takes output previously generated by Devin and returns it as-is
// This is used as a grader to evaluate the markdown output

const { spawnSync } = require('child_process');

const prompt = process.argv[2];
const options = process.argv[3];
const context = process.argv[4];

console.log(prompt);
